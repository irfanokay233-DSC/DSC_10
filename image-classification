## Image Classification (CNN)

This project uses a convolutional neural network to classify images from a benchmark dataset.

The goal is to understand how deep learning models perform and how model design choices affect results.
